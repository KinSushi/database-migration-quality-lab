# Performance Notes

## Current scope

The project intentionally uses small synthetic datasets.

## Future scaling topics

- staging tables;
- batched loads;
- indexing natural keys;
- partitioning large transactional tables;
- source-to-target checksum validation;
- query-plan review with `EXPLAIN`.
